---
description: Ejecuta las pruebas del stack afectado
---

Ejecuta las pruebas de $ARGUMENTS con el comando de tests propio del proyecto (detecta el stack por su manifiesto o configuración; usa el skill del stack si existe).

Estos comandos compilan: pide autorización si no la tienes. Reporta evidencia: comando exacto, resultado y fallos con `ruta:línea`. Nunca deshabilites un test para que pase.
