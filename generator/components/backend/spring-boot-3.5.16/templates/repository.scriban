package com.{{ Company }}.{{ MicroserviceName }}.infrastructure.persistence.repositories;

import com.{{ Company }}.{{ MicroserviceName }}.infrastructure.persistence.entities.{{ EntityCapital }}Entity;
import org.springframework.data.repository.reactive.ReactiveCrudRepository;
import org.springframework.stereotype.Repository;

@Repository
public interface {{ EntityCapital }}Repository extends ReactiveCrudRepository<{{ EntityCapital }}Entity, Long> {
}