package com.{{ Company }}.{{ MicroserviceName }}.infrastructure.adapters;

import com.{{ Company }}.{{ MicroserviceName }}.domain.model.{{ EntityCapital }};
import com.{{ Company }}.{{ MicroserviceName }}.infrastructure.persistence.models.{{ EntityCapital }}PersistenceModel;
import com.{{ Company }}.{{ MicroserviceName }}.infrastructure.persistence.providers.IProviderPersistence;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import reactor.core.publisher.Mono;
import reactor.test.StepVerifier;

import static org.mockito.Mockito.*;

public class {{ EntityCapital }}AdapterTest {

    private {{ EntityCapital }}Adapter {{ Entity }}Adapter;
    private IProviderPersistence<{{ EntityCapital }}PersistenceModel> {{ Entity }}Repository;

    private final Long {{ Entity }}Id = 1L;
    private final {{ EntityCapital }} {{ Entity }} = new {{ EntityCapital }}({{ Entity }}Id);
    private final {{ EntityCapital }}PersistenceModel {{ Entity }}PersistenceModel = new {{ EntityCapital }}PersistenceModel({{ Entity }}Id);

    @BeforeEach
    public void setup() {
        {{ Entity }}Repository = mock(IProviderPersistence.class);

        {{ Entity }}Adapter = new {{ EntityCapital }}Adapter({{ Entity }}Repository);
    }

    @Test
    public void testGet{{ Entity }}Id_Success() {
        when({{ Entity }}Repository.findById({{ Entity }}Id)).thenReturn(Mono.just({{ Entity }}PersistenceModel));

        Mono<{{ EntityCapital }}> result = {{ Entity }}Adapter.get{{ EntityCapital }}Id({{ Entity }}Id);

        StepVerifier.create(result)
                .expectNextMatches(actual{{ EntityCapital }} ->
                        actual{{ EntityCapital }}.id().equals({{ Entity }}.id()))
                .verifyComplete();

        verify({{ Entity }}Repository, times(1)).findById({{ Entity }}Id);
    }

    @Test
    public void testGet{{ Entity }}Id_{{ Entity }}NotFound() {
        when({{ Entity }}Repository.findById({{ Entity }}Id)).thenReturn(Mono.empty());

        Mono<{{ EntityCapital }}> result = {{ Entity }}Adapter.get{{ EntityCapital }}Id({{ Entity }}Id);

        StepVerifier.create(result)
                .expectError(RuntimeException.class)
                .verify();

        verify({{ Entity }}Repository, times(1)).findById({{ Entity }}Id);
    }
}