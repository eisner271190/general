{{ if(Name == "security") }}package com.{{ Company }}.{{ Name }}.application.mappers;

import com.{{ Company }}.{{ Name }}.application.dto.RecoverPasswordRequestDTO;
import com.{{ Company }}.{{ Name }}.domain.model.RecoverPassword;
import org.mapstruct.Mapper;
import org.mapstruct.factory.Mappers;

@Mapper
public interface RecoverPasswordMapperDto {
    RecoverPasswordMapperDto INSTANCE = Mappers.getMapper(RecoverPasswordMapperDto.class);

    RecoverPasswordRequestDTO toDTO(RecoverPassword model);
    RecoverPassword toModel(RecoverPasswordRequestDTO dto);
}
{{ end }}