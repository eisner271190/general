{{ if(Name == "security") }}package com.{{ Company }}.{{ Name }}.domain.ports;

import com.{{ Company }}.{{ Name }}.domain.model.*;

import reactor.core.publisher.Mono;

public interface ExchangePort {
    Mono<ExchangeResponse> exchangeCodeForTokens(ExchangeRequest request);
}
{{ end }}
